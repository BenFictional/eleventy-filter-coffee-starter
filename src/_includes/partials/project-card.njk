<li class="project card 

  {% for item in post.data.project_type -%}
    {{ item | slug }}
  {%- endfor %}
  
  {% for item in post.data.media %}
    {{ item | replace(" ", "-") }}
  {% endfor %}
">

      {# HEADER #}
      <ul class="project-meta">
        {# TYPE #}
        <li>{{ post.data.project_type }}</li>

        {# LOCATION #}
        {% if post.data.location %}
          <li>{{ post.data.location }}, </li>
        {% endif %}

        {# YEAR #}
        <li><time class="post-date" datetime="{{ post.date | htmlDateString }}">{{ post.date | yearOnly }}</time></li>

      </ul>

      {# IMAGE #}
      <a class="featured-image" href ="{{ post.url | url }}"><img  src="{{ post.data.featured_image}}" alt="{{ post.data.featured_alt }}"></a>

      <div class="project-text">
      
        {# TITLE #}
        {% if post.data.project_type.includes("life") %}
            <h2>{{ post.data.title }}</h2>
        {% else %}
            <a class="project-title" href="{{ post.url | url }}">{{ post.data.title }}</a>
        {% endif %}

        {# MEDIA #}
        <p class="media-list">
           {% set comma = joiner("/") %}
           {% for item in post.data.media %}
              {{ comma() }} {{ item | capitalize }}
          {% endfor %}
        </p>
       

        {# DESCRIPTION #}
        <span class="post-description">{{ post.data.description }}</span>

      </div>

    </li>